#cloud-config
hostname: ${vm_name}

users:
  - name: ${CLOUD_USER}
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
    ssh_authorized_keys:
      - ${DEV_SSH_KEY}

package_update: true
package_upgrade: false
