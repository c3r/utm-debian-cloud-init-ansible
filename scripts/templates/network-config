version: 2
ethernets:
  ${NETWORK_INTERFACE}:
    dhcp4: false
    dhcp6: false
    accept-ra: false
    addresses:
      - ${vm_ip}/24
    routes:
      - to: default
        via: ${GATEWAY}
    nameservers:
      addresses: ${DNS_INLINE}
